/**
 * Repo donde publicás los instaladores del POS (Electron).
 * No uses el repo de esta landing — usá el de la app.
 */
window.GITHUB_RELEASE_CONFIG = {
  owner: "MLA-ex3tav",
  repo: "MLAapp",
};
